```markdown
# 🔔 Reminder Bot

**Простой и мощный бот-напоминалка для Telegram и VK**

## 🎯 О проекте

**Reminder Bot** — это асинхронный бот для Telegram и VK, который помнит о важных событиях и отправляет вам уведомления в нужное время.

Бот создан с использованием лучших практик:
- ✅ **Command Pattern** — чистая архитектура команд
- ✅ **Repository Pattern** — отделение логики от БД
- ✅ **Dependency Injection** — гибкое управление зависимостями
- ✅ **Async/Await** — асинхронная обработка
- ✅ **APScheduler** — надёжное планирование задач

## ⚠️ Важно

**Запуск возможен только одного экземпляра бота на приложение.**  
Не запускайте несколько копий приложения (например, несколько процессов или контейнеров с одинаковыми токенами) одновременно. Это может привести к дублированию уведомлений и конфликтам при обработке напоминаний.

---

## ⚡ Возможности

### Основной функционал
- ✅ **Создание напоминаний** с разными форматами времени
- ✅ **Списки напоминаний** — просмотр всех активных напоминаний
- ✅ **Отмена напоминаний** — удалить по ID
- ✅ **Приоритеты** — low, medium, high
- ✅ **Повторения** — once, daily, weekly, monthly

### Форматы времени
```text
18:00                    → Сегодня в 18:00
завтра 15:30            → Завтра в 15:30
через 2 часа            → Через 2 часа
через 30 минут          → Через 30 минут
2024-11-20 14:00        → Конкретная дата и время
```

### Примеры команд
```text
/remind Купить молоко | 18:00
/remind Встреча | завтра 15:30 | HIGH | once
/remind Работа | 09:00 | MEDIUM | daily
/reminders
/cancel_reminder 5
```

---

## 📦 Требования

- **Python 3.13+**
- **PostgreSQL 12+**
- **Telegram Bot Token**

### Зависимости

Основные библиотеки:

| Пакет | Версия | Назначение |
|-------|--------|------------|
| aiogram | ≥3.26.0 | Асинхронный фреймворк для Telegram Bot API |
| apscheduler | ≥3.11.2 | Планировщик задач |
| sqlalchemy | ≥2.0.48 | ORM для работы с БД |
| alembic | ≥1.18.4 | Миграции базы данных |
| asyncpg | ≥0.31.0 | Асинхронный драйвер PostgreSQL |
| psycopg2 | ≥2.9.11 | Синхронный драйвер PostgreSQL (для утилит) |
| pydantic | ≥2.12.5 | Валидация данных и настройки (с расширением email) |
| pydantic-settings | ≥2.13.1 | Управление конфигурацией |
| python-dotenv | ≥1.2.2 | Загрузка переменных окружения из .env |
| pytz | ≥2026.1.post1 | Работа с часовыми поясами |
| tzlocal | ≥5.3.1 | Определение локального часового пояса |
| pyyaml | ≥6.0.3 | Работа с YAML-файлами (например, для конфигов) |
| dataclasses | ≥0.8 | Обратная совместимость (встроен в Python 3.7+) |

**Тестовые зависимости:**
- `pytest >= 9.0.2` — фреймворк для тестирования
- `pytest-asyncio >= 1.3.0` — поддержка асинхронных тестов
- `freezegun >= 1.5.5` — подмена времени в тестах
- `virtualenv >= 21.2.0` — утилита для создания виртуальных окружений (разработка)

**Для разработки (dev):**
- `ruff >= 0.15.5` — линтер и форматтер кода

---

## 💻 Использование

### Основные команды

#### `/start`
Приветствие и справка по использованию

```text
👋 Привет! Я бот-напоминалка.

📝 Основные команды:
/remind - Создать напоминание
/reminders - Показать все напоминания
/cancel_reminder - Отменить напоминание
/help - Справка
```

#### `/remind <текст> | <время> [| приоритет] [| повтор]`
Создать новое напоминание

**Примеры:**
```text
/remind Купить молоко | 18:00

/remind Встреча с командой | завтра 15:30

/remind Позвонить маме | через 2 часа

/remind Рабочая встреча | 09:00 | HIGH | daily

/remind Купить подарок | 2024-11-20 19:00 | MEDIUM | once
```

**Поддерживаемые форматы времени:**
- `HH:MM` — Сегодня в указанное время (если прошло → на завтра)
- `завтра HH:MM` — Завтра в указанное время
- `через X часов` — Через X часов
- `через X минут` — Через X минут
- `YYYY-MM-DD HH:MM` — Конкретная дата и время

**Приоритеты:**
- `LOW` — Низкий приоритет
- `MEDIUM` — Средний приоритет (по умолчанию)
- `HIGH` — Высокий приоритет

**Повторения:**
- `ONCE` — Один раз (по умолчанию)
- `DAILY` — Каждый день в указанное время
- `WEEKLY` — Каждую неделю
- `MONTHLY` — Каждый месяц

#### `/reminders`
Показать все ваши напоминания

```text
📋 Ваши напоминания:

#1 📝 Купить молоко
⏰ 16.11.2025 18:00
🔔 medium | 🔄 once
━━━━━━━━━━━

#2 📝 Встреча с командой
⏰ 17.11.2025 15:30
🔔 high | 🔄 daily
━━━━━━━━━━━
```

#### `/cancel_reminder <ID>`
Отменить напоминание по ID

```text
/cancel_reminder 1
✅ Напоминание отменено
```

#### `/help`
Получить справку

---

## 📝 Примеры

### Пример 1: Простое напоминание

```text
Пользователь пишет:
/remind Купить молоко | 18:00

Ответ бота:
✅ Напоминание создано!

📝 Купить молоко
⏰ 16.11.2025 18:00
🔔 medium
🔄 once

[Через время...]
🔔 НАПОМИНАНИЕ!

📝 Купить молоко
⏰ 16.11.2025 18:00
🔔 MEDIUM
```

### Пример 2: Повторяющееся напоминание

```text
Пользователь пишет:
/remind Встреча с командой | 09:00 | HIGH | daily

Ответ бота:
✅ Напоминание создано!

📝 Встреча с командой
⏰ 16.11.2025 09:00
🔔 high
🔄 daily

[Каждый день в 09:00 бот отправляет напоминание]
```

### Пример 3: Относительное время

```text
Пользователь пишет:
/remind Позвонить маме | через 30 минут | HIGH

Ответ бота:
✅ Напоминание создано!

📝 Позвонить маме
⏰ 16.11.2025 18:45 (текущее время + 30 минут)
🔔 high
🔄 once
```

### Пример 4: Список напоминаний

```text
Пользователь пишет:
/reminders

Ответ бота:
📋 Ваши напоминания:

#1 📝 Купить молоко
⏰ 16.11.2025 18:00
🔔 medium | 🔄 once

#2 📝 Встреча
⏰ 17.11.2025 15:30
🔔 high | 🔄 daily

#3 📝 Позвонить маме
⏰ 16.11.2025 18:45
🔔 high | 🔄 once
```

### Пример 5: Отмена напоминания

```text
Пользователь пишет:
/cancel_reminder 1

Ответ бота:
✅ Напоминание отменено
```

---

## 🧪 Разработка

### Установка для разработки

```bash
# Клонирование репозитория
git clone <url>
cd bot-reminder

# Создание виртуального окружения (Python 3.13+)
python -m venv venv
source venv/bin/activate      # Linux/macOS
# venv\Scripts\activate       # Windows

# Установка проекта в режиме редактирования с dev-зависимостями
# Рекомендуется использовать uv (быстрая альтернатива pip):
# uv pip install -e ".[dev]"

# Если используете стандартный pip (версия 24.2+ поддерживает dependency-groups):
pip install -e ".[dev]"
```

### Использование just для управления проектом

Для удобства в проекте используется **`just`** — командный раннер, который позволяет быстро выполнять часто используемые задачи. Команды определены в файле `justfile`.

#### Установка just

- **Windows:** `winget install --id Casey.Just --exact` или `scoop install just`
- **macOS:** `brew install just`
- **Linux:** используйте пакетный менеджер (например, `apt install just` для Ubuntu) или установите из [релизов](https://github.com/casey/just/releases)

#### Доступные команды

Выполните `just` или `just --list`, чтобы увидеть список команд:

```text
just dev       # Запуск бота в режиме разработки (ENVIRONMENT=development)
just prod      # Запуск бота в продакшн-режиме (ENVIRONMENT=production)
just lint      # Запуск ruff check --fix
just format    # Запуск ruff format
```

**Примеры:**

```bash
# Запуск в режиме разработки
just dev

# Запуск в продакшн-режиме
just prod

# Проверка и автоисправление кода
just lint

# Форматирование кода
just format
```

### Запуск тестов

```bash
pytest

# Для запуска конкретного теста:
pytest tests/test_parser.py::test_parse_time
```

## 📊 Логирование

Логирование в проекте настраивается с помощью YAML-файлов конфигурации. В зависимости от окружения (`ENVIRONMENT=development` или `ENVIRONMENT=production`) загружается соответствующий файл:

- **`log_conf.development.yaml`** — используется при разработке (уровень `DEBUG`, логи пишутся в консоль и в файлы в папке `logs/vk_logs/dev/` и `logs/tg_logs/dev/`).
- **`log_conf.production.yaml`** — используется в продакшне (уровень `INFO`, логи пишутся в консоль и в файлы в корневых папках `logs/vk_logs/prod/` и `logs/tg_logs/prod/`).

---

Спасибо за использование! 🚀
```
